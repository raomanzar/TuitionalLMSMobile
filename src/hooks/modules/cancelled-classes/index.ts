/**
 * Public surface of the cancelled-classes-module hooks.
 * Import from `'@/hooks/modules/cancelled-classes'` — internal files are implementation detail.
 */
export {
  useFilteredCancelledClasses,
  countActiveCancelledClassFilters,
} from './useFilteredCancelledClasses';
