/**
 * Public surface of the auth-module hooks.
 * Import from `'@/hooks/modules/auth'` — internal files are implementation detail.
 */
export {
  authQueryKeys,
  useConfirmPasswordMutation,
  useForgotPasswordMutation,
  useResetPasswordMutation,
  useSignInMutation,
} from "./authQueries";
